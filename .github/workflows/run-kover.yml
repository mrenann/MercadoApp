name: Android Kover Test Coverage

on:
  pull_request:
    branches: [ master ]

permissions:
  pull-requests: write
  contents: read

jobs:
  test-coverage:
    name: Generate Test Coverage Report
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run tests with Kover
        run: |
          ./gradlew test koverXmlReport --stacktrace

      - name: Store XML report
        uses: actions/upload-artifact@v4
        with:
          name: kover-xml-report
          path: build/reports/kover/xml/report.xml

      - name: Extract coverage percentage
        id: kover-report
        run: |
          echo "Procurando relatórios em todos os módulos:"
          find . -name "report.xml" | grep -i kover

          echo "Conteúdo dos diretórios de relatórios:"
          find . -path "*/build/reports/kover*" -type d | xargs ls -la 2>/dev/null || echo "Nenhum diretório encontrado"

          if [ -d "app/build/reports/kover" ]; then
            echo "Encontrado diretório de relatório no módulo app:"
            ls -la app/build/reports/kover/
          fi
          
          
          REPORT_FILES=$(find . -name "report.xml" | grep -i kover)
          
          if [ -z "$REPORT_FILES" ]; then
            echo "❌ Nenhum arquivo report.xml encontrado"
            COVERAGE_PERCENTAGE="NA"
          else
            TOTAL_COVERED=0
            TOTAL_MISSED=0
          
            echo "Processando os seguintes relatórios:"
            echo "$REPORT_FILES"

            for REPORT_PATH in $REPORT_FILES; do
              echo "Analisando relatório: $REPORT_PATH"
              echo "Conteúdo do relatório:"
              cat "$REPORT_PATH"

              LINE_COVERED=$(grep -o 'type="LINE".*covered="[0-9]*"' "$REPORT_PATH" | grep -o 'covered="[0-9]*"' | grep -o '[0-9]*' | awk '{ sum += $1 } END { print sum }')
              LINE_MISSED=$(grep -o 'type="LINE".*missed="[0-9]*"' "$REPORT_PATH" | grep -o 'missed="[0-9]*"' | grep -o '[0-9]*' | awk '{ sum += $1 } END { print sum }')
          
              echo "Relatório $REPORT_PATH - linhas cobertas: $LINE_COVERED, linhas não cobertas: $LINE_MISSED"

              TOTAL_COVERED=$((TOTAL_COVERED + LINE_COVERED))
              TOTAL_MISSED=$((TOTAL_MISSED + LINE_MISSED))
            done
          
            echo "Total combinado - Linhas cobertas: $TOTAL_COVERED, Linhas não cobertas: $TOTAL_MISSED"

            TOTAL_LINES=$((TOTAL_COVERED + TOTAL_MISSED))
          
            if [ "$TOTAL_LINES" -eq 0 ]; then
              echo "⚠️ Nenhuma linha de código para testar encontrada em todos os relatórios."
              COVERAGE_PERCENTAGE="NA"
            else
              TOTAL_COVERAGE=$(echo "scale=4; $TOTAL_COVERED / $TOTAL_LINES" | bc)
              COVERAGE_PERCENTAGE=$(echo "scale=2; $TOTAL_COVERAGE * 100" | bc -l)
              echo "📊 Cobertura calculada: ${COVERAGE_PERCENTAGE}%"
            fi
          fi
          
          echo "coverage=$COVERAGE_PERCENTAGE" >> $GITHUB_OUTPUT

          {
            echo "## Relatório de Cobertura de Código"
            echo
            echo "### Resumo"
            echo "* Total de linhas cobertas: $TOTAL_COVERED"
            echo "* Total de linhas não cobertas: $TOTAL_MISSED"
            echo "* Total de linhas: $TOTAL_LINES"
            echo "* Porcentagem de cobertura: ${COVERAGE_PERCENTAGE}%"
            echo
            echo "### Relatórios Encontrados"
            echo "$REPORT_FILES"
            echo
            echo "### Conteúdo dos Relatórios"
          
            for REPORT_PATH in $REPORT_FILES; do
              echo
              echo "#### $REPORT_PATH"
              echo '```xml'
              cat "$REPORT_PATH"
              echo '```'
            done
          } > coverage-report.md
          
          echo "debug_report=coverage-report.md" >> $GITHUB_OUTPUT

      - name: Comment coverage on PR
        if: github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: test-coverage
          message: |
            ## 📊 Cobertura de Código
            
            **Cobertura Total: ${{ steps.kover-report.outputs.coverage == 'NA' && 'N/A - Sem código para testar' || format('{0}%', steps.kover-report.outputs.coverage) }}**
            
            <details>
              <summary>Detalhes de Cobertura</summary>
            
              Este relatório foi gerado pelo Kover v0.9.1
            
              ${{ steps.kover-report.outputs.coverage == 'NA' && '⚠️ **Aviso**: Nenhuma linha de código encontrada para teste. Isto pode indicar que:' || '' }}
              ${{ steps.kover-report.outputs.coverage == 'NA' && '- O projeto não contém código testável' || '' }}
              ${{ steps.kover-report.outputs.coverage == 'NA' && '- Kover não está configurado para encontrar o código-fonte' || '' }}
              ${{ steps.kover-report.outputs.coverage == 'NA' && '- Os testes não estão sendo executados corretamente' || '' }}
            
              ${{ steps.kover-report.outputs.coverage != 'NA' && 'Para melhorar a cobertura:' || '' }}
              ${{ steps.kover-report.outputs.coverage != 'NA' && '- Adicione testes para funções e classes não testadas' || '' }}
              ${{ steps.kover-report.outputs.coverage != 'NA' && '- Assegure que fluxos condicionais estejam cobertos' || '' }}
              ${{ steps.kover-report.outputs.coverage != 'NA' && '- Verifique que exceções e casos de erro são testados' || '' }}
            </details>

      - name: Upload debug report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-debug-report
          path: coverage-report.md

      - name: Check coverage threshold
        run: |
          MIN_COVERAGE=70
          COVERAGE="${{ steps.kover-report.outputs.coverage }}"
          
          if [ "$COVERAGE" == "NA" ]; then
            echo "⚠️ Não foi possível calcular a cobertura - sem linhas de código para testar"
            # Optionally, you can make this a warning but not a failure
            # exit 0
            # Or make it fail:
            echo "::error::⚠️ Sem linhas de código testáveis encontradas. Verifique a configuração do Kover."
            exit 1
          elif (( $(echo "$COVERAGE < $MIN_COVERAGE" | bc -l) )); then
            echo "::error::⚠️ Cobertura abaixo do limite de ${MIN_COVERAGE}% (Atual: ${COVERAGE}%)"
            exit 1
          else
            echo "✅ Cobertura acima do limite de ${MIN_COVERAGE}%"
          fi