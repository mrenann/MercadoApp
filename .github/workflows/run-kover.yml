name: Android Kover Test Coverage

on:
  pull_request:
    branches: [ master ]

permissions:
  pull-requests: write
  contents: read

jobs:
  test-coverage:
    name: Generate Test Coverage Report
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run tests with Kover
        run: ./gradlew koverXmlReport

      - name: Store XML report
        uses: actions/upload-artifact@v4
        with:
          name: kover-xml-report
          path: build/reports/kover/xml/report.xml

      - name: Extract coverage percentage
        id: kover-report
        run: |
          REPORT_PATH="build/reports/kover/xml/report.xml"
          
          if [ ! -f "$REPORT_PATH" ]; then
            echo "❌ Coverage report not found at $REPORT_PATH"
            exit 1
          fi
          
          echo "✅ Found report at: $REPORT_PATH"
          
          # Extrai o line-rate usando sed
          TOTAL_COVERAGE=$(sed -n 's/.*<coverage[^>]*line-rate="\([^"]*\)".*/\1/p' "$REPORT_PATH")
          
          echo "DEBUG - TOTAL_COVERAGE: $TOTAL_COVERAGE"
          
          if [ -z "$TOTAL_COVERAGE" ]; then
            echo "❌ Couldn't extract coverage from XML!"
            exit 1
          fi
          
          # Calcula a porcentagem e formata com 2 casas decimais
          COVERAGE_PERCENTAGE=$(echo "scale=2; $TOTAL_COVERAGE * 100" | bc -l)
          echo "📊 Coverage: ${COVERAGE_PERCENTAGE}%"
          echo "coverage=$COVERAGE_PERCENTAGE" >> $GITHUB_OUTPUT

      - name: Comment coverage on PR
        if: github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: test-coverage
          message: |
            ## 📊 Cobertura de Código
            
            **Cobertura Total: ${{ steps.kover-report.outputs.coverage }}%**
            
            <details>
              <summary>Detalhes de Cobertura</summary>
            
              Este relatório foi gerado pelo Kover v0.9.1
            
              Para melhorar a cobertura:
              - Adicione testes para funções e classes não testadas
              - Assegure que fluxos condicionais estejam cobertos
              - Verifique que exceções e casos de erro são testados
            </details>

      - name: Upload full report
        uses: actions/upload-artifact@v4
        with:
          name: kover-full-report
          path: build/reports/kover/

      - name: Check coverage threshold
        run: |
          MIN_COVERAGE=70
          COVERAGE=${{ steps.kover-report.outputs.coverage }}
          
          if (( $(echo "$COVERAGE < $MIN_COVERAGE" | bc -l) )); then
            echo "::error::⚠️ Cobertura abaixo do limite de ${MIN_COVERAGE}% (Atual: ${COVERAGE}%)"
            exit 1
          else
            echo "✅ Cobertura acima do limite de ${MIN_COVERAGE}%"
          fi